import { Link } from 'gatsby'

import { stitches } from '../stitches'

export const Menu = {
  Container: stitches.styled('div', {}),
  Button: stitches.styled('button', {}),
  Link: stitches.styled(Link, {}),
}
