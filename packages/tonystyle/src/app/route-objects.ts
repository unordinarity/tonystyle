import { aboutHome } from 'src/pages/about-home'
import { blogHome } from 'src/pages/blog-home'
import { home } from 'src/pages/home'
import { notFound } from 'src/pages/not-found'
import { projectsHome } from 'src/pages/projects-home'

export const routeObjectsMap = {
  aboutHome,
  blogHome,
  home,
  notFound,
  projectsHome,
}

export const routeObjectsList = [
  aboutHome,
  blogHome,
  home,
  notFound,
  projectsHome,
]
