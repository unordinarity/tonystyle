import paper from './paper.png'
import noise from './noise.png'

export const background = {
  images: {
    paper: `url(${paper})`,
    noise: `url(${noise})`,
  },
  themeMap: {
    backgroundImage: 'image',
  },
}
